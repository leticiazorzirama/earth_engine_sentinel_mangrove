/**** Start of imports. If edited, may not auto-convert in the playground. ****/
var image = ee.Image("projects/leticiazr/assets/babiDegr1_bloco2_chuvoso_2016-12-06_a_2016-12-06");
/***** End of imports. If edited, may not auto-convert in the playground. *****/
// Visualizacao em cor natural
var visParams = {
  bands: ['B4', 'B3', 'B2'],
  min: 0,
  max: 0.3,   // Adjust max (e.g., 2000-4000) based on your region's brightness
  gamma: 1.4   // Adjust gamma (e.g., 1.2-1.5) to enhance contrast in darker features
};

Map.addLayer(image, visParams, 'Natural color');
Map.centerObject(image)

// NDVI
var visNdvi = {
    bands: ['ndvi'],          // NDVI median band.
    min: 0,                          // Minimum NDVI value for visualization.
    max: 1,                          // Maximum NDVI value for visualization.
    palette: 'ff0000,ffff00,00aa00', // Gradient from red (low NDVI) to green (high NDVI).
    format: 'png'
};

Map.addLayer(image, visNdvi, 'NDVI');
Map.centerObject(image)

//MMRI
var visMmri = {
    bands: ['ndvi'],          // MMRI median band.
    min: 0,                          // Minimum MMRI value for visualization.
    max: 1,                          // Maximum MMRI value for visualization.
    palette: 'ff0000,ffff00,00aa00', // Gradient from red (low MMRI) to green (high MMRI).
    format: 'png'
};

Map.addLayer(image, visMmri, 'MMRI');
Map.centerObject(image)


