/**** Start of imports. If edited, may not auto-convert in the playground. ****/
var degrad = ee.FeatureCollection("projects/leticiazr/assets/babitonga_degradadas"),
    preserv = ee.FeatureCollection("projects/leticiazr/assets/babitonga_preservadas");
/***** End of imports. If edited, may not auto-convert in the playground. *****/
// ETAPA: CARREGAR, IMPORTAR E VISUALIZAR ASSETS

// Áreas degradadas 
Map.centerObject(degrad);
Map.addLayer(degrad, {color: 'red', fillColor: 'ff000033'}, 'Babitonga_degradada');

// Áreas preservadas
Map.centerObject(preserv);
Map.addLayer(preserv, {color: 'green', fillColor: '#6bb300'}, 'Babitonga_preservada');

// ETAPA: AQUISITAR IMAGENS

